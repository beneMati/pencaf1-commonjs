doctype html 
html 
  head 
    title= title 
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
  body  
    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
      a.navbar-brand(href="#") 
      button.navbar-toggler(type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation")
        span.navbar-toggler-icon
      .collapse.navbar-collapse#navbarNavAltMarkup
        if role === 'admin'
          .navbar-nav
            a.nav-item.nav-link.active(href="/app/scores") Scores
            a.nav-item.nav-link(href="/app/predictions") Predictions
            a.nav-item.nav-link(href="/admin/results") Results
            a.nav-item.nav-link(href="/admin/drivers") Drivers
        else if role === 'user'
          .navbar-nav
            a.nav-item.nav-link.active(href="/app/scores") Scores
            a.nav-item.nav-link(href="/app/predictions") Predictions

    block content 

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")